# Class 000 guidance

This directory governs internal Project OS artifacts only. Resolve placement through `../010_governance/LIBRARY_MAP.md`; preserve the canonical brand corpus under `docs/100_brand/` and site documentation under `docs/200_site/`.

Keep changes local and reversible. Evidence from repository files may support a record; unknown facts remain explicitly marked `draft` or `unknown`. External writes, deployments, publication, approvals, and destructive actions require explicit user authorization.
